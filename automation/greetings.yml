name: Greetings on: [pull request target, issues] jobs: greeting: runs-on: Ubuntu-latest permissions: issues: write pull-requests: write steps: - uses: actions/first interaction@v1 with: repo-token: $GITHUB_TOKEEN issue-message: Message that will be display on users first issue pr-message: message that will be displyed on users pull requestname: Greetings on: [pull request target, issues] jobs: greeting: runs-on: Ubuntu-latest permissions: issues: write pull-requests: write steps: - uses: actions/first intername: Greetings
on: [pull-request target, issues]:
jobs:
  greeting: runs on: Ubuntu-Latest
permissions: issues:
write pull-requests
steps:
- uses: actions/github-script@v1
with:
repo-token: ${{ secrets.GITHUB_TOKEN }}
issue-message: "Message that will be displayed on users' first issue"
pr-message: "${{ steps.run-action.outputs.pr-number }}"action@v1 with: repo-token: $GITHUB_TOKEEN issue-message: Message that will be display on users first issue pr-message: message that will be displyed on users pull request
